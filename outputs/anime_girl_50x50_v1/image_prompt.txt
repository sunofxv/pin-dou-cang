请把这张照片重绘为一幅按 50 列 × 50 行 (共 2500 个方块) 排列的像素画, 并且显式画出网格线。

技术要求:
1. 每个像素方块的内部必须是单一纯色, 没有渐变、没有描边、没有阴影。
2. 相邻方块之间用一条浅灰色细网格线 (大约 1-2 像素宽, RGB 接近 (200, 200, 200)) 分隔。
3. 整张图严格按 50 列 × 50 行等大网格排列, 网格线必须笔直、等宽。
4. 不要在方块里写数字、色号或任何文字, 不要画格外的装饰、边框或花边。

为什么要画网格线: 后续程序会按格扫描取色, 网格线让每格边界清晰, 可以避免相邻格颜色被错混。

请把主体卡通化:增强边缘对比、简化阴影、轮廓清晰,适合做潮玩拼豆。

保留原图背景。

整张图最多使用约 18 种不同颜色;相近颜色请合并成同一个色块,优先表达主体而不是层次。

最终输出: 仅一张 PNG 像素色块图,不要附加说明文字。